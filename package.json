{
  "name": "colorindex",
  "version": "1.0.0",
  "description": "Color Index",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "debug-server": "node inspect server/index.js ",
    "heroku-postbuild": "npm install --only=dev --no-shrinkwrap && npm run build-json && npm run build",
    "dev": "tsc --watch && concurrently \"nodemon server/index.js\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/parser": "^4.7.0",
    "concurrently": "^5.3.0",
    "eslint": "^7.13.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-prettier": "^3.1.4",
    "faker": "^5.1.0",
    "prettier": "^2.1.2",
    "process": "^0.11.10",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "airtable": "^0.10.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
